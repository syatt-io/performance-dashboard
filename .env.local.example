# Local Development Environment (mirrors Digital Ocean production)
# Copy this to .env.local and fill in your actual values
# Usage: Use with docker-compose.dev.yml for local testing

# PRODUCTION SETTINGS (from Digital Ocean):
# - PostgreSQL 15 (db-s-1vcpu-1gb, 1GB RAM, 10GB disk, NYC1)
# - Redis: Upstash Redis with TLS
# - App Region: Toronto (tor1)
# - Instance: professional-xs (512MB RAM, 0.5 vCPU)

# Database (Docker PostgreSQL 15 - matches production)
DATABASE_URL=postgresql://performance_user:local_dev_password@localhost:5432/performance_dashboard

# Redis (Docker Redis 7 - local, no TLS unlike production Upstash)
# Production uses: rediss://default:password@famous-newt-14974.upstash.io:6379
REDIS_URL=redis://localhost:6379

# Server (matches production)
PORT=3000
NODE_ENV=development

# Security (use production key or generate a 32-char random string)
ENCRYPTION_KEY=local-dev-encryption-key-32-chars!!
ALLOWED_ORIGINS=http://localhost:3001,http://localhost:3000

# Google PageSpeed API
# Get your own API key from: https://console.cloud.google.com/apis/credentials
# Enable the PageSpeed Insights API in your Google Cloud project
PAGESPEED_API_KEY=your_google_pagespeed_api_key_here

# WebPageTest API (optional - production doesn't use this)
WEBPAGETEST_API_KEY=

# Monitoring (disabled by default for local dev)
LIGHTHOUSE_INTERVAL_HOURS=24
MONITORING_ENABLED=false
MONITORING_HOUR=2

# Frontend (matches production URL format)
NEXT_PUBLIC_API_URL=http://localhost:3000